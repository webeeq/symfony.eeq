{% trans_default_domain 'FOSUserBundle' %}

<h2>Profil</h2>
<table class="fos_user_user_show">
    <tr>
        <td>{{ 'profile.show.username'|trans }}</td>
        <td>{{ user.username }}</td>
    </tr>
    <tr>
        <td>{{ 'profile.show.email'|trans }}</td>
        <td>{{ user.email }}</td>
    </tr>
</table>
