{# templates/login-user/login-user.html.twig #}
{% extends 'layout.html.twig' %}

{% block title %}Logowanie{% endblock %}

{% block record %}
    <h2>Logowanie</h2>
    <p>Opcja ta pozwala na zalogowanie użytkownika do systemu. Proszę podać login i hasło do swojego konta. Jeśli nie pamiętają Państwo hasła, system prześle na adres e-mail nowe. Można zapamiętać hasło w systemie, aby w przyszłości nie musieć się ponownie logować.</p>
    {% if message %}
        {{ message|raw }}
    {% endif %}
    {{ form_start(form) }}
        <table>
            <tr>
                <td>Login:</td>
                <td>{{ form_widget(form.login) }}</td>
            </tr>
            <tr>
                <td>Hasło:</td>
                <td>{{ form_widget(form.password) }}</td>
            </tr>
            <tr>
                <td>&nbsp;</td>
                <td>{{ form_widget(form.forget) }} Nie pamiętam hasła</td>
            </tr>
            <tr>
                <td>&nbsp;</td>
                <td>{{ form_widget(form.remember) }} Zapamiętaj hasło</td>
            </tr>
            <tr>
                <td>&nbsp;</td>
                <td>{{ form_widget(form.save, { 'label': 'Zatwierdź' }) }}</td>
            </tr>
        </table>
    {{ form_end(form) }}
{% endblock %}
